---
import MediaTile from './MediaTile.astro';

interface Props {
  href?: string;
  title: string;
  meta?: string;
  src?: string;
}

const { href, title, meta, src } = Astro.props;
---

<MediaTile href={href} title={title} meta={meta} src={src} />
